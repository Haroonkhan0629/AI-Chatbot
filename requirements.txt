# Web server
fastapi
uvicorn[standard]
python-dotenv
# Serverless adapter — imported at module level by the function files
mangum
# PDF processing & database
pypdf
sqlmodel
pg8000
# HTTP client
requests
pydantic
# Optional local dev only — install manually to use on-device embeddings:
#   pip install sentence-transformers numpy
# Without them the app falls back to the HuggingFace Inference API automatically.
